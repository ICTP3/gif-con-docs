name: Notify Discord (Pull Request)

on:
  pull_request:
    types: [opened, closed, reopened]

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4
        
      - name: 🚀 Discordへ通知
        env:
          WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_PR }}
        run: bash scripts/send_pr.sh
